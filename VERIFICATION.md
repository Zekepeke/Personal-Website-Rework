# Claim Verification

Every claim below was checked against the actual repository contents (README, source,
tests, benchmarks, commit history) via the GitHub MCP on 2026-09-01. Nothing was rewritten
or softened — claims are only classified.

## Repo name resolution

The requested names did not all match real repositories. `Zekepeke` has **33** public repos
(the claim text says 34).

| Requested name | Actual repo | Note |
|---|---|---|
| `reachy-mini` | **NOT FOUND** | No repo by that name. Closest: `Reachy-Server-Project` (whose README says "my DIY Reachy Mini–style project") and `ReachyCloneWebApp`. Claims mapped to `Reachy-Server-Project`. |
| `machine-learn` | **NOT FOUND** | No repo by that name. ML-adjacent repos: `Computer-Vision-Project`, `First-Nueral-Network`, `autograd`, `moldmany`. Claims mapped to `Computer-Vision-Project`. |
| `price-is-right-glasses` | `Price-is-right` | Repo name is `Price-is-right`; its README title is "Price is Right Glasses". |
| `BLK1800` | `Team-Project-BLK1800` | Matched. |
| `Shop22` | `shop22` | Matched (also a separate near-empty `Shop22-revamped`). |
| `AirCursor` | **NOT FOUND** | No repo by that name. The cursor-control code lives inside `Computer-Vision-Project` (`pyautogui`). |
| `ZekeShot` | `Zekeshot` | Matched. **It is not a screenshot utility — see below.** |

Claims referencing projects outside the seven requested names were also verified, since the
brief asks for one row per claim.

## Verification table

| claim | repo | status | evidence |
|---|---|---|---|
| **Voice Assistant ReSpeak** — stack includes "Whisper (OpenAI)" | voice-assistant-respeaker | CONTRADICTED | STT is `faster-whisper` run **remotely** on a separate RUBIK Pi 3 board over HTTP, not OpenAI's API. `config.py` §SPEECH-TO-TEXT: `WHISPER_MODEL = "base.en"`, `WHISPER_COMPUTE_TYPE = "int8"`, reached via `RUBIKPI_STT_PORT = 8000`. `requirements.txt` has no `openai` package. |
| Integrates "Google Gemini for advanced reasoning" | voice-assistant-respeaker | CONTRADICTED | The LLM is Phi-3 Mini / Dolphin via `llama-server` on the RUBIK Pi 3 — `README.md` execution-flow step 4 ("Phi-3 Mini Q4 via llama-server"), and the RUBIK Pi setup section launches `Dolphin3.0-Llama3.1-8B-Q4_K_M.gguf`. `config.py` does define `GEMINI_API_KEY` / `GEMINI_MODEL = "gemini-2.5-flash"` (used for the image path), but reasoning is not Gemini-served. |
| Integrates ElevenLabs for voice synthesis | voice-assistant-respeaker | VERIFIED | `requirements.txt` line 20 `elevenlabs>=1.0.0`; `config.py` §TEXT-TO-SPEECH: `ELEVENLABS_VOICE_ID`, `ELEVENLABS_MODEL_ID = "eleven_turbo_v2_5"`, `ELEVENLABS_OUTPUT_FORMAT = "pcm_16000"`. |
| Real-time audio processing / wake word | voice-assistant-respeaker | VERIFIED | `requirements.txt`: `sounddevice>=0.4.6`, `openwakeword>=0.6.0`, `onnxruntime>=1.15.0`; `config.py` §WAKE WORD sets `WAKE_WORD_MODEL = "hey_jarvis"`, 80 ms / 1280-sample chunks; `test_wakeword.py` exists. |
| "Eliminates latency constraints… minimal processing overhead"; "no noticeable delays" | voice-assistant-respeaker | UNSUPPORTED | No timing harness, benchmark, or measured number anywhere in the repo. The only latency material is a prose section in `README.md` ("Why latency stays low") with zero measurements. |
| "Engineered efficient request/response buffering and streaming protocols" | voice-assistant-respeaker | VERIFIED | Streaming design is real and described concretely: `README.md` step 5 — LLM SSE tokens → sentence buffer → ElevenLabs Turbo v2.5 → `sounddevice` playback, plus parallel audio/camera capture threads in step 2. (The *latency claim* attached to it is the row above.) |
| "Hardware-aware memory management on ReSpeaker devices, enabling real-time inference on constrained edge hardware" | voice-assistant-respeaker | CONTRADICTED | Inference is **offloaded**, not run on the edge device: STT and LLM both run on a separate RUBIK Pi 3 over HTTP (`config.py` `RUBIKPI_HOST = "http://192.168.4.23"`), and TTS is the ElevenLabs cloud. The ReSpeaker in `README.md`'s hardware table is a USB microphone array, not a compute target. No memory-management code exists. |
| **Computer Vision Project** — stack: OpenCV, NumPy, PyTorch, Jupyter | Computer-Vision-Project | VERIFIED | `requirements.txt` lines 1–6: `mediapipe~=0.10.21`, `opencv-python~=4.8.0`, `numpy~=1.24.2`, `pyautogui~=0.9.54`, `torch~=2.0.1`; `LaunchPadProject-2.ipynb` at repo root. |
| Stack includes "TensorFlow" and "Scikit-learn" | Computer-Vision-Project | CONTRADICTED | Neither appears in `requirements.txt` (full contents are the six lines quoted above). The DL framework is PyTorch, and the vision layer is MediaPipe. |
| "State-of-the-art deep learning models for object detection and segmentation" | Computer-Vision-Project | CONTRADICTED | The project is hand-**gesture** recognition on MediaPipe landmarks. `README.md`: "gesture recognition using Mediapipe, OpenCV, and neural networks… Train a neural network on labeled landmarks". Assets are `hand_landmarker.task` + `mediapipeLive.py` / `mediapipeVideo.py`. No detector, no segmentation model. |
| "Production-grade accuracy metrics" | Computer-Vision-Project | UNSUPPORTED | No accuracy figure, eval script, or test set in the repo; `README.md` has no results section at all. |
| "Reducing processing time by 60–70% compared to naive implementations" | Computer-Vision-Project | UNSUPPORTED | No benchmark, profiling script, or timing output exists. The number appears nowhere in the repo — not even in the README. |
| "Supports… batch image processing" | Computer-Vision-Project | UNSUPPORTED | Only two entry points exist: `mediapipeLive.py` (webcam stream) and `mediapipeVideo.py` (video file). Nothing iterates a directory of images. |
| **Reachy Server** — stack "Flask/FastAPI" | Reachy-Server-Project | CONTRADICTED (partial) | FastAPI is present: `server/app.py:1` `from fastapi import FastAPI, Request`; `requirements.txt` `fastapi==0.116.1`, `uvicorn[standard]==0.30.6`. Flask appears nowhere in the repo. |
| Stack: OpenCV, Raspberry Pi 5, RESTful API | Reachy-Server-Project | VERIFIED | `requirements.txt`: `opencv-python-headless==4.8.0.76`, `mediapipe==0.10.5`, `torch==2.0.1`; `server/app.py` imports `picamera2` and exposes `@app.get("/mjpeg")`; `README.md` documents the Pi 5 / libcamera apt setup. |
| "Real-time bi-directional communication via WebSockets" | Reachy-Server-Project | CONTRADICTED | There is no WebSocket anywhere. The stream is one-way HTTP MJPEG: `server/app.py` returns `StreamingResponse(gen(), media_type="multipart/x-mixed-replace; boundary=frame")`. The only route in the file is `GET /mjpeg`. |
| "Low-latency control of robotic arms" | Reachy-Server-Project | CONTRADICTED | The server has no control endpoint of any kind — `server/app.py` defines exactly one route (`/mjpeg`) plus startup/shutdown hooks. `README.md`'s feature list and Future Work mention only video, gesture recognition, and a future LLM/TTS agent. No arm, motor, or actuator code exists. |
| "Maintaining 30+ FPS performance with concurrent model inference" | Reachy-Server-Project | UNSUPPORTED | No FPS measurement or benchmark. `server/app.py` configures 1280×720 with `buffer_count=2` and annotates once per frame in `MJPEGHub.write()`, but never asserts or measures a frame rate; `README.md` states no performance figure. |
| "Deployed containerized microservices architecture with Docker" | Reachy-Server-Project | CONTRADICTED | No `Dockerfile` and no `docker-compose.yml` in the repo (root contains only `README.md`, `requirements.txt`, `model/`, `server/`, `.venv` symlink, `.DS_Store`). `README.md` documents a manual `python -m uvicorn server.app:app` run on the Pi. |
| **Shop22** — "Built a full-stack e-commerce platform from ground up" | shop22 | CONTRADICTED | It is the official Payload template, unmodified in structure. `README.md:1`: "# Payload E-Commerce Template" / "This is the official Payload E-Commerce Template". `package.json:2`: `"name": "@payloadcms/template-ecommerce"`, `"description": "E-commerce template for Payload"`. Commit history (16 commits, Jul 2024) is cosmetic: "changed static image", "deleted all the glbs and finalize code", "added prettier back", "fixed accesss deny bs". |
| Stack: "MERN… Node.js, MongoDB, Express.js" | shop22 | VERIFIED (partial) | Express and MongoDB are present but via the template: `package.json` `"express": "^4.19.2"`, `"@payloadcms/db-mongodb": "^1.0.0"`. React is present as part of Next.js 13, not a standalone MERN SPA. |
| Stack: "Redux, Material-UI" | shop22 | CONTRADICTED | Neither is a dependency. `package.json` has no `redux`, `@reduxjs/toolkit`, or `@mui/*`. State is `react-hook-form` + Payload; UI is the template's own SCSS. |
| Stack: "TypeScript, Stripe API" | shop22 | VERIFIED | `package.json`: `"typescript": "^4.8.4"`, `"stripe": "^10.2.0"`, `"@payloadcms/plugin-stripe": "^0.0.19"`, `"@stripe/react-stripe-js"`, plus a `stripe:webhooks` script. |
| "Server-side rendering optimization… sub-second page load times" | shop22 | UNSUPPORTED | No measurement in the repo. Note the template's own `README.md` §Cache states Next.js caching "is disabled by default" via `no-store` and `export const dynamic = 'force-dynamic'` — the opposite of the claimed optimization, and no commit changes that. |
| "PCI-compliant transaction handling and comprehensive error recovery mechanisms for failed payments" | shop22 | UNSUPPORTED | The Stripe flow is the template's stock `POST /api/create-payment-intent` documented in `README.md` §Checkout. No custom payment-failure handling was added in any commit, and "PCI-compliant" is asserted nowhere in the repo. |
| "Reducing average response times from 800ms to 120ms" via MongoDB indexing + caching layers | shop22 | UNSUPPORTED | No benchmark, no index definitions, no cache layer, and no such numbers anywhere in the repo. They appear only in the claim text. |
| **Personal Website Rework** — stack "Next.js, TypeScript" | Personal-Website-Rework | CONTRADICTED | It is a Vite + JavaScript app. `package.json:2` `"name": "vite-project"`; scripts are `"dev": "vite"`, `"build": "vite build"`. No `next` dependency, no `typescript` dependency, no `next.config.*`, no `tsconfig.json`; entry point is `index.html` + `src/main.jsx`. |
| Stack: React, Tailwind, Vercel | Personal-Website-Rework | VERIFIED | `package.json`: `"react": "^18.3.1"`, `"tailwindcss": "^3.4.13"`, `tailwind.config.js` at root; `.vercel/` directory and Vercel-style serverless functions in `api/` (`now-playing.js`, `top-tracks.js`, `_spotify.js`). |
| "Server-side rendering and incremental static regeneration" | Personal-Website-Rework | CONTRADICTED | Vite builds a client-side SPA; there is no SSR or ISR mechanism in the project. No framework capable of ISR is installed. |
| "Lighthouse scores of 95+ across all metrics" | Personal-Website-Rework | UNSUPPORTED | A repo-wide grep for "lighthouse" (excluding `node_modules`) returns nothing — no report, no config, no CI step, and the number is not in `README.md` (which is still the stock "React + Vite" template README). |
| "Reducing initial page load from 3.5s to 0.8s" via next-gen image formats and lazy loading | Personal-Website-Rework | UNSUPPORTED | No measurement artifact of any kind in the repo, and no image-optimization pipeline (no `next/image`, no build-time image plugin in `package.json`). |
| "CI/CD pipeline… instant preview deployments for every commit" | Personal-Website-Rework | UNSUPPORTED | No `.github/` directory and no CI config in the repo. The `.vercel/` link means Vercel's Git integration could provide previews, but nothing in the repo evidences a configured pipeline. |
| **BLK1800** — stack "Spring Boot, PostgreSQL, JWT, RESTful APIs" | Team-Project-BLK1800 | CONTRADICTED | Plain Java over raw sockets, no framework. GitHub code search for `springframework`/`jwt`/`postgresql` across the repo returns **0 results**. `README.md` run instruction is `java -cp /Path/To/The/Project/out/production/Team-Project-BLK1800 src.App`. Networking is `src/SocketIO.java` (`write()`, `read()`, `sendHandShake()`), and `src/Server/Server.java`. |
| "Spring Boot microservices with layered architecture (controller, service, repository)" | Team-Project-BLK1800 | CONTRADICTED | `src/` is flat: `App.java`, `User.java`, `Message.java`, `ConversationReader.java`, `SocketIO.java`, plus `client/`, `gui/`, `Server/`. No controller/service/repository layering. `README.md` §"Running Everything" describes manually starting two `main` methods, not services. |
| "JWT-based authentication with role-based access control (RBAC)" | Team-Project-BLK1800 | CONTRADICTED | Auth is username/password validated over the socket: `README.md` documents `ClientSide.validUserAndPassword()` — "Validates usernames and passwords based on specific rules". No token, no roles, no permissions model in the tree. |
| "Optimized database performance with strategic indexing and query optimization… complex feed aggregation queries" | Team-Project-BLK1800 | CONTRADICTED | There is no database. Persistence is flat files: top-level `USER_DATABASE/` and `MESSAGE_DATABASE/` directories plus loose `bob.txt` / `null.txt`. `README.md` §Messages describes "file storage… based on the specified file path". There is no feed feature either — the app is friends + direct messages. |
| "Supporting thousands of concurrent users" | Team-Project-BLK1800 | UNSUPPORTED | No load test, no concurrency benchmark, no such figure in the repo. |
| "…for maintainability and testability across multiple features" | Team-Project-BLK1800 | VERIFIED (tests only) | A real test suite exists: `tests/` holds `UserTest.java`, `MessageTest.java`, `ServerTest.java`, `ClientCommunicationHandlerTest.java`, `ConversationReaderTest.java`, `GUIForAppTest.java`, `UpdateUsersTest.java`, plus `tests/clientAndServer/`. The *architecture* half of the claim is the row above. |
| **Facial Recognition server** — "dual-framework REST API supporting both Flask and FastAPI with identical functionality" | facial-recognition-flask-or-fastapi | CONTRADICTED | Only Flask exists. `server/app.py:1` `from flask import Flask, request, jsonify`; `server/app.py:6` `app = Flask(__name__)`; `server/` contains only `app.py`, `face_db.json`, `requirements.txt`. `README.md` line 2 states the choice was never made: "using flask or fastapi(haven't made a choice yet)". |
| Stack: "face_recognition library" | facial-recognition-flask-or-fastapi | CONTRADICTED | Detection is MediaPipe and embedding is keras-facenet: `server/app.py:3` `import mediapipe as mp`, `:4` `from keras_facenet import FaceNet`, then `embedder = FaceNet()  # 512-D embeddings`. The `face_recognition` package is not used. |
| Stack: OpenCV, RESTful APIs | facial-recognition-flask-or-fastapi | VERIFIED | `server/app.py:2` `import cv2, …`; routes `@app.route("/enroll/<person_id>", methods=["POST"])`, `@app.route("/identify", methods=["POST"])`, `@app.route("/whoami", methods=["GET"])`, exercised by the `curl` examples in `README.md`. |
| Stack: CORS | facial-recognition-flask-or-fastapi | CONTRADICTED | `server/app.py` is the entire server and registers no CORS handling (`flask_cors` is never imported, no `after_request` header code). The React frontend in `README.md` is pointed at the API via `VITE_API_BASE_URL` with no CORS layer server-side. |
| "Sub-50ms inference times per face detection on standard hardware" | facial-recognition-flask-or-fastapi | UNSUPPORTED | No timing code, benchmark, or profiling anywhere. The number appears in no file. |
| "Optimized… with efficient model loading and batch processing" | facial-recognition-flask-or-fastapi | CONTRADICTED (partial) | Model loading is a single module-level `embedder = FaceNet()`, so that half holds. Batch processing does not exist: `/enroll` and `/identify` each read exactly one `request.files["file"]`, and `embed_face()` calls `embedder.embeddings([face_rgb_160])[0]` — a one-item list. Matching is a linear scan over every stored embedding in `face_db.json`. |
| "Production-ready server… robust stability under high request volumes"; "comprehensive error handling and input validation" | facial-recognition-flask-or-fastapi | CONTRADICTED | Error handling is real but basic (missing-`file` 400s and `try/except` around preprocessing in both routes). "Production-ready" is contradicted by the last line of `server/app.py`: `app.run(host="0.0.0.0", port=8000, debug=True)` — the Flask development server with debug on. No WSGI server, no load test. |
| **ZekeShot** — "Screenshot Utility… desktop utility for screenshot capture, editing, and organization" | Zekeshot | CONTRADICTED | **ZekeShot is a Pygame arcade game, not a screenshot tool.** `README.md:1-2`: "# ZekeShot / ZekeShot is a pixel Pygame shooter where the user blast upward through waves of enemies to collect even **more** chickens." Entire codebase is one file, `zekeShot.py`, with classes `Bullet`, `Chicken`, `Player`, `Refill`, `Enemy`, `LevelBuilder`, `Game`, `Menu`. |
| Stack: "Tkinter, PIL (Pillow)" | Zekeshot | CONTRADICTED | `requirements.txt` contains exactly one line: `pygame`. `zekeShot.py:1-3` imports `sys, math, random, time`, `pygame`, and `from pygame import Vector2, sprite, mixer`. Neither Tkinter nor PIL is imported anywhere. |
| "Responsive GUI with Tkinter, real-time preview and drag-and-drop" | Zekeshot | CONTRADICTED | The UI is a Pygame surface: `screen = pygame.display.set_mode((800, 800))`, and the `Menu`/`Game` loops handle only `pygame.QUIT`, `MOUSEBUTTONDOWN`, and `K_SPACE`. No preview, no drag-and-drop. |
| "Optimized image processing pipeline for fast screenshot capture and export, supporting multiple formats and compression levels" | Zekeshot | CONTRADICTED | The only image work is loading and scaling game sprites from `data/images/` (`Player.png`, `Chicken.png`, `Explosion.png`, `Fox.png`, `EnemyZeke.png`, `Ammo.png`, `Logo.png`) via `pygame.image.load(...).convert_alpha()`. Nothing captures or exports an image. |
| "Intelligent file organization with automatic metadata extraction and folder-based categorization" | Zekeshot | CONTRADICTED | The only file I/O in the program is the high-score file: `open("data/scores/highscore.csv", "r")` in `Menu.update()` and `Player.check_state()`, which also writes it back. No metadata, no categorization. |
| **Virtual Cards** — "collaborative digital business card platform… create, share, manage virtual contact cards" | virtual-cards | UNSUPPORTED | Nothing in the repo describes business or contact cards. The only app is `frontend/luv-note/`, `package.json:2` `"name": "luv-notes"`. The GitHub repo description is just "Project idea with a good friend". |
| Stack: TypeScript, React, Material-UI | virtual-cards | VERIFIED | `frontend/luv-note/package.json`: `"typescript": "^5"`, `"react": "19.2.3"`, `"@mui/material": "^7.3.6"`, `"@mui/icons-material": "^7.3.6"` (alongside a full Radix/shadcn set). |
| Stack: "Node.js, Express.js, MongoDB, Stripe" | virtual-cards | CONTRADICTED | There is no backend directory — the repo root is `README.md`, `LICENSE`, `.gitignore`, `frontend/`. No Express, no MongoDB, no Stripe in `frontend/luv-note/package.json`. Data is Supabase: `"@supabase/supabase-js": "^2.89.0"`, `"@supabase/ssr": "^0.8.0"`. The app is Next.js 16 (`"next": "16.1.1"`), which the claim omits. |
| "Full-stack TypeScript application with end-to-end type safety" | virtual-cards | CONTRADICTED | Only a frontend exists (see row above), so there is no end-to-end path to type. |
| "Encrypted storage and OAuth 2.0 integration, ensuring compliance with data protection regulations" | virtual-cards | UNSUPPORTED | Code search for `oauth` across the repo returns 0 results. Supabase is installed but no auth provider configuration, encryption code, or compliance artifact is present. |
| "Real-time card sharing via WebSocket connections… instantaneous updates across multiple user sessions" | virtual-cards | CONTRADICTED | Code search for `websocket` and `realtime` across the repo returns **0 results**. Supabase Realtime is not wired up. |
| **Web Scraping for GDG** — stack: BeautifulSoup, Requests | webscrapping-for-GDG | VERIFIED | `crawler/basic_crawler.py:1` `import requests`, `:4` `from bs4 import BeautifulSoup`; same pair at the top of `crawler/dynamic_crawler.py`. |
| Stack: "Selenium… dynamic rendering with Selenium" (multiple scraping strategies) | webscrapping-for-GDG | CONTRADICTED | Code search for `selenium` across the repo returns **0 results**. Despite its name, `crawler/dynamic_crawler.py` is also plain `requests` + BeautifulSoup — it differs only by using DFS and writing to Firebase (`from firebase import firebase_write`). No JS rendering exists. |
| "Rate limiting and respectful request handling, avoiding server overload" | webscrapping-for-GDG | VERIFIED | `crawler/basic_crawler.py` calls `time.sleep(0.5)` after each page inside the crawl loop, plus a `max_depth`/`pages_crawled` cap and a `visited` set; `crawler/dynamic_crawler.py` does the same with `MAX_LINKS = 30` and `max_pages`. |
| "Adhering to robots.txt guidelines" | webscrapping-for-GDG | CONTRADICTED | No `robots.txt` fetch or `urllib.robotparser` usage anywhere (search returns 0 hits). Both crawlers enqueue every same-domain link found and `requests.get()` it unconditionally — `basic_crawler.py` filters only on `parsed_url.netloc.endswith("purdue.edu")` and path depth. |
| "Structured output formats (JSON, CSV)" | webscrapping-for-GDG | CONTRADICTED (partial) | JSON is real: both crawlers `json.dump(output, …)` to `policies.json` (534 KB committed at root). No CSV writer exists in the repo. |
| **Price is Right** — "iOS game inspired by the classic television game show… complex game mechanics" | Price-is-right | CONTRADICTED | It is a resale-price scanner for Meta Ray-Ban glasses, not a game. `README.md:1-3`: "# Price is Right Glasses / Point your Meta Ray-Ban glasses at any vintage or second-hand item and instantly know if it is worth buying. Powered by Claude or Gemini Vision, with multi-source pricing across eBay, Discogs, and trading card APIs." |
| Stack: Swift, SwiftUI, Xcode, iOS | Price-is-right | VERIFIED | A SwiftUI iOS app is present at `frontend/meta-wearables-dat-ios/samples/CameraAccess/` — e.g. `CameraAccess/CameraAccessApp.swift`, `Views/StreamSessionView.swift`, `Views/VerdictView.swift`, `ViewModels/StreamSessionViewModel.swift`. Note it is built on Meta's Wearables sample app, not written from scratch. |
| Stack omits the backend entirely (claimed stack is "Swift, SwiftUI, Xcode, iOS Development, Game Logic") | Price-is-right | CONTRADICTED | Most of the repo is a Python FastAPI service: `backend/main.py:6` `from fastapi import FastAPI`, with `backend/vision/{claude,gemini}.py`, `backend/pricing/{ebay,discogs,tcg}.py`, `backend/tts/elevenlabs.py`, and Supabase persistence (`supabase.table("scans").insert(...)`). None of this appears in the claimed stack. |
| "Real-time score calculation and winner determination algorithms supporting multiple simultaneous players" | Price-is-right | CONTRADICTED | There is no score, winner, or multiplayer concept. The nearest logic is a single-user price verdict in `backend/main.py`: `verdict = "Great deal" if median < 20 else "Fair price" if median < 60 else "Overpriced"`, with `net_profit = median - (median * 0.1325) - 5`. |
| "Consistent 60 FPS performance across different iOS device generations" | Price-is-right | UNSUPPORTED | No performance measurement, instrument trace, or FPS figure in the repo; `README.md` makes no performance claim. |
| **FastAPI Learning Foundation** — stack: Python, FastAPI, OpenAPI/Swagger docs | fastapi | VERIFIED | `myapi.py:1` `from fastapi import FastAPI, Path`, `:3` `app = FastAPI()`. Swagger UI is FastAPI's built-in default (no repo code required, and none was written). |
| Stack: "Pydantic, Async/Await"; "input validation using Pydantic models" | fastapi | CONTRADICTED | No Pydantic model is defined — request bodies are untyped dicts: `def create_student(student_id: int, student: dict)` and `def update_student(student_id: int, student: dict, name: Optional[str] = None)`. `BaseModel` is never imported. |
| "Asynchronous API endpoints with FastAPI's built-in async support, achieving 2–3x higher throughput" | fastapi | CONTRADICTED | Every endpoint in `myapi.py` is a synchronous `def` — `root`, `get_student`, `get_student_by_name`, `create_student`, `update_student`, `delete_student`. No `async`/`await` in the file. The "2–3x" figure has no benchmark and appears nowhere in the repo. |
| "Production-ready configurations including CORS handling, authentication middleware, and structured error responses" | fastapi | CONTRADICTED | `myapi.py` is 63 lines with no middleware, no CORS, and no auth. Errors are returned as HTTP 200 bodies — `return {"error": "Student not found"}` — rather than structured `HTTPException` responses. The whole repo is `myapi.py`, `requirements.txt`, and a 147-byte README ("Ground works for learning fastapi"). |
| **Chrome Extension Demo** — stack: content scripts, background service workers | chrome-demo | VERIFIED | `manifest.json`: `"manifest_version": 3`, `"content_scripts": [{ "matches": ["http://*/*","https://*/*"], "js": ["content.js"] }]`, `"background": { "service_worker": "background.js" }`, plus `options_page` and a popup action. |
| "Chrome storage APIs for persistent user preferences… with proper synchronization" | chrome-demo | VERIFIED | `chrome.storage.sync` is used in all three scripts: `options.js` (`get(["displayName","backendUrl"])` / `set({displayName, backendUrl})`), `background.js` (`onInstalled` defaults + `chrome.storage.onChanged` broadcast), `popup.js` (`get(["status"])` + `onChanged` listener). |
| "Efficient communication patterns, minimizing performance impact on browser operations" | chrome-demo | UNSUPPORTED | No profiling or measurement of any kind; the extension is ~8 KB of scripts with no performance notes (`README.md` is 14 bytes). |
| "Optimized extension packaging and deployment for Chrome Web Store with comprehensive permission declarations and security best practices" | chrome-demo | CONTRADICTED | No packaging step exists — `package.json` is 48 bytes with no build script, and `node_modules/` is committed to the repo. Permissions are maximally broad rather than minimal: `"host_permissions": ["http://*/*", "https://*/*"]` plus content-script matches on all HTTP(S) pages. Nothing indicates a Web Store submission. |
| **Demo Raspberry Pi Project** — stack "Python, Raspberry Pi OS, GPIO Control" | ReachyCloneWebApp | CONTRADICTED | The repo contains no Python and no GPIO code. It is a Vite + React web UI: `camera-ui/package.json` `"name": "camera-ui"`, `"dev": "vite"`, deps `react ^19.1.1` / `react-dom ^19.1.1`; repo root is `.env`, `README.md` (70 bytes), `package-lock.json` (85 bytes), `camera-ui/`. |
| "Efficient system communication protocols… with automatic reconnection and offline buffering" | ReachyCloneWebApp | UNSUPPORTED | No reconnection or buffering logic is present; the app is a small client UI for the `Reachy-Server-Project` MJPEG endpoint. |
| "Secure remote access with certificate-based authentication and encrypted communication channels" | ReachyCloneWebApp | CONTRADICTED | No auth or TLS code exists. The paired server it talks to is deliberately open: `Reachy-Server-Project/server/app.py` sets `CORSMiddleware(allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])` and serves plain HTTP on port 8000. |
| **Summary** — "I've retrieved all 34 of your repositories" | (account-wide) | CONTRADICTED | `user:Zekepeke` returns **33** repositories, none of them forks. |
| **Summary** — portfolio includes "neural networks" | (account-wide) | VERIFIED | Separate repos exist for this: `First-Nueral-Network` (Python), `autograd` (Jupyter), plus the landmark classifier in `Computer-Vision-Project`. |
| **Summary** — portfolio includes "Docker containerization, CI/CD pipelines" | (account-wide) | UNSUPPORTED | The only Docker artifacts found are `Dockerfile` / `docker-compose.yml` inside `shop22`, which ship with the Payload template. No CI workflow file was found in any repo inspected. |

## Notable things found that are NOT in the claims

1. **`Computer-Vision-Project` is a hand-gesture mouse controller.** `requirements.txt` includes
   `pyautogui~=0.9.54  # For GUI automation` and `mediapipeLive.py` imports `import pyautogui as pag`.
   This is almost certainly what the "AirCursor" name in the request refers to — controlling the
   OS cursor from hand landmarks. It is the most distinctive thing in the repo and the claim set
   describes it as generic "object detection and segmentation" instead.

2. **`Price-is-right` has a real, non-trivial pricing architecture** the claims completely miss:
   a barcode-first path in `backend/main.py` (`extract_barcode` → `lookup_barcode`) that queries
   **OpenFoodFacts, OpenLibrary, and UPCitemdb** before falling back to vision; a pricing router
   with automatic eBay fallback when Discogs or TCG return zero results (`get_prices_with_fallback`);
   and a swappable Claude/Gemini vision provider behind one env var. Its `README.md` is also
   unusually honest about limits — e.g. eBay Browse returns "asking prices, not sold", Discogs
   `/marketplace/stats` makes low/high/median equal in v1, and holofoil-only Pokémon cards return 0.

3. **`voice-assistant-respeaker/config.py` contains a hard-coded sexually explicit LLM system
   prompt** (`LLM_SYSTEM_PROMPT`, a "Lady Bella" persona described as degrading and unfiltered),
   committed to a public repository. Flagging it because it is public and would be found by anyone
   reading the repo.

4. **`Team-Project-BLK1800` stores user and message data as plain files in the repo**, including
   committed `bob.txt` and `null.txt` at root alongside `USER_DATABASE/` and `MESSAGE_DATABASE/`.
   It also has a genuinely reasonable JUnit test suite (9 test files) — the one part of the
   "testability" claim that holds up.

5. **`facial-recognition-flask-or-fastapi` commits `face_db.json` (33 KB of face embeddings)**
   to the repository. These are FaceNet 512-D vectors of enrolled people, not raw images, but they
   are biometric-derived data in public source control.

6. **`shop22` is unmodified template code**, but `Shop22-revamped` (141 KB, last pushed 2025-07-11)
   exists as a separate repo — neither the claims nor the request mention it.

7. **`Zekeshot` has a known bug worth noting** if it is ever presented as a work sample:
   `Chicken.explode()` mutates `self.explosions` with `.remove()` while iterating over it, and
   `Bullet.draw()` reloads and rescales `data/images/Explosion.png` from disk on every frame for
   every active bullet.

8. **`Reachy-Server-Project` does one genuinely good thing** the claims don't mention: `MJPEGHub`
   in `server/app.py` annotates each frame exactly once and fans it out to all connected clients
   via a `Condition` + sequence number, so per-client cost stays flat. That is a real design
   decision — unlike the WebSocket/Docker/30-FPS claims attached to the same project.
